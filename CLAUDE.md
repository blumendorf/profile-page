# Claude Entry Point

1. Read **[AGENTS.md](AGENTS.md)** — project entrypoint and index of `agent-context/`.
2. Open only the linked docs under `agent-context/` that match the task (see the table in `AGENTS.md`).

Optional: [`.claude/settings.json`](.claude/settings.json) for **permissions** only. Do not duplicate project conventions there.

Do not use this file as a second instruction set beyond pointing to `AGENTS.md` and `agent-context/`.
